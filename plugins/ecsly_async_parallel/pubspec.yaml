name: ecsly_async_parallel
description: ECS-aware parallel task systems built on async_parallel.
version: 0.1.0
homepage: https://github.com/arenukvern/ecsly/plugins/ecsly_async_parallel

environment:
  sdk: ">=3.12.0 <4.0.0"

resolution: workspace
dependencies:
  async_parallel:
  ecsly:

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.8
  xsoulspace_lints: ^0.1.2